[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "orion-rustpotter"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

[tool.maturin]
module-name = "orion_rustpotter"
